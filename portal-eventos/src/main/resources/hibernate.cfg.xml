<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Ligação à base de dados -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/hibernateDemo?useSSL=false&amp;serverTimezone=UTC</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">Csfatic2015</property>

        <!-- Dialeto MySQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Mostra SQL no console -->
        <property name="hibernate.show_sql">true</property>

        <!-- Atualiza automaticamente as tabelas -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapeamento das entidades -->
        <mapping class="portaleventos.TipoEvento"/>

    </session-factory>
</hibernate-configuration>
