<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- ============================== -->
        <!-- Database Connection Settings -->
        <!-- ============================== -->

        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/projetoLp?createDatabaseIfNotExist=true</property>
        <property name="connection.username">root</property>
        <property name="connection.password">Ze_Pedro100</property>
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- ============================== -->
        <!-- Hibernate Behavior Settings -->
        <!-- ============================== -->

        <property name="hibernate.hbm2ddl.auto">update</property>
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- ============================== -->
        <!-- Mapped Entity Classes -->
        <!-- ============================== -->

        <mapping class="portaleventos.Pedido"/>
        <mapping class="portaleventos.Evento"/>
        <mapping class="portaleventos.Exposicao"/>
        <mapping class="portaleventos.Festa"/>
        <mapping class="portaleventos.Palestra"/>
        <mapping class="portaleventos.Participante"/>
        <mapping class="portaleventos.Person"/>
        <mapping class="portaleventos.Promotor"/>
        <mapping class="portaleventos.Workshop"/>

    </session-factory>
</hibernate-configuration>
